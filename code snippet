class Math {
public:
	static int Add(int a, int b) const {
		return a + b;
	}
};

int main() {
	Math m;
	int result = m.Add(1, 2);
	result = Math::Add(3, 4);
	return 0;
}

/*
- static member function cannot be constant member function.
*/
